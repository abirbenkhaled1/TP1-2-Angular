export class CarteVisite {
    nom: string = ''; // Initialize with an empty string
    prenom: string = ''; // Initialize with an empty string
    entreprise: string = ''; // Initialize with an empty string
    email: string = ''; // Initialize with an empty string
    telephone: string = ''; // Initialize with an empty string

    constructor() {
        this.nom = '';
        this.prenom = '';
        this.entreprise = '';
        this.email = '';
        this.telephone = '';
    }
}
